when a user adds an excel sheet add all records to database where user name is kora 
then get all the rows that have the same date as previously added rows and add those 
except the ones that already have been added